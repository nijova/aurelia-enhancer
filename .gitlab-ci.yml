image: node:latest

build-job:
  stage: build
  script:
    - npm install
    - npm run build

pages:
  stage: deploy
  script:
  - ls
  - mkdir .public
  - cp -r * .public
  - mv .public public
  - ls
  - ls public
  artifacts:
    paths:
    - public
  only:
  - master
