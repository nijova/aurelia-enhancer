image: node:latest

pages:
  stage: deploy
  script:
  - npm install
  - npm run build
  - ls
  - mkdir .public
  - cp -r * .public
  - mv .public public
  - ls
  - ls public
  artifacts:
    paths:
    - public
  only:
  - master
